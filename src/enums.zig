pub const GridItem = enum {
    blank,
    food,
    snake,
};

pub const Direction = enum {
    north,
    south,
    east,
    west,
};

pub const Key = enum {
    up,
    down,
    left,
    right,
    reset,
    quit,
};
